package com.model.channel.ao;

import com.model.Task;
import com.model.channel.ChannelManager;

public class AOChannelManager extends ChannelManager {

	public AOChannelManager(Task task) {
		super(task);
	}
}
